# Database Configuration
DATABASE_URL=localhost
DATABASE_PORT=3306
DATABASE_NAME=lms_db
DATABASE_USER=lmsuser
DATABASE_PASSWORD=lmspassword

# Multi-Database Configuration (for Read/Write separation)
DATABASE_ROUTING_ENABLED=false  # Set to true to enable Read/Write separation
DATABASE_REPLICA_URL=localhost  # Replica (Slave) database host
DATABASE_REPLICA_PORT=3307      # Replica (Slave) database port
DATABASE_REPLICA_USER=lmsuser   # Optional, defaults to DATABASE_USER
DATABASE_REPLICA_PASSWORD=lmspassword  # Optional, defaults to DATABASE_PASSWORD

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Mail Configuration (Gmail example)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=ddingsha9@teambind.co.kr
MAIL_PASSWORD= 
MAIL_FROM=your-ddingsha9@teambind.co.kr
MAIL_FROM_NAME=LMS System

# Security Configuration
JWT_SECRET=your-very-long-secret-key-at-least-256-bits-long-for-hs256-algorithm
JWT_ACCESS_TOKEN_EXPIRATION_TIME=360000
JWT_REFRESH_TOKEN_EXPIRATION_TIME=2592000000
ENCRYPTION_KEY=your-aes-256-encryption-key-32-characters-long!
ENCRYPTION_IV:defaultIvParam456

# Server Configuration
SERVER_PORT=8080
