apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-management-service.fullname" . }}
  labels:
    {{- include "course-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-management-service.selectorLabels" . | nindent 4 }}
