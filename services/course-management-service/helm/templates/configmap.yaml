apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "course-management-service.fullname" . }}-config
  labels:
    {{- include "course-management-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
