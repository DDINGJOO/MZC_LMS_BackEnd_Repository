apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "organization-service.fullname" . }}-config
  labels:
    {{- include "organization-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
