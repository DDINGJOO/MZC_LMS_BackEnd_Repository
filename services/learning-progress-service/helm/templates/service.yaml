apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-progress-service.fullname" . }}
  labels:
    {{- include "learning-progress-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learning-progress-service.selectorLabels" . | nindent 4 }}
