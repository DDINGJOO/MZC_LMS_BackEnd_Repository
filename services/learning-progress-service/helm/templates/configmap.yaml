apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "learning-progress-service.fullname" . }}-config
  labels:
    {{- include "learning-progress-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
