apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "user-profile-service.fullname" . }}-config
  labels:
    {{- include "user-profile-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
