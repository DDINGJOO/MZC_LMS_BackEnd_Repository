apiVersion: v1
kind: Secret
metadata:
  name: {{ include "user-profile-service.fullname" . }}-secrets
  labels:
    {{- include "user-profile-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
