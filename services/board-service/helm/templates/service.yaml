apiVersion: v1
kind: Service
metadata:
  name: {{ include "board-service.fullname" . }}
  labels:
    {{- include "board-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "board-service.selectorLabels" . | nindent 4 }}
